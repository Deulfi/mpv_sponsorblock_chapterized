server=https://sponsor.ajay.app/api/skipSegments
# Categories to fetch and skip types are: sponsor, selfpromo, interaction, intro, outro, preview, music_offtopic
# preview, poi_highlight, filler, exclusive_access
# use '_' instead of ' '
categories=sponsor,selfpromo,interaction,Self_Promotion
# Categories that are only marked, not skipped
show_only_cats=intro, outro, music_offtopic, preview, poi_highlight, filler, exclusive_access
# Set this to "true" to use sha256HashPrefix instead of videoID
hash=
# Duration the OSD message shows up (in seconds)
show_msg_duration=3
# should the script skip unknown categories (not in categories or show_only_cats)? It will show them only otherwise.
skip_unknown=no
# Generate uosc button?
uosc_button=yes
# show badge count for sponsorblock segments
show_sponsor_count=yes
# Button info for uosc direct
button_enabled_icon=shield
button_disabled_icon=remove_moderator
button_tooltip=Sponsorblock
# Send button info directly to uosc or via other script? Ignore is you don't know. (e.g. plugin of uosc_controls_modifier)
use_ucm_plugin=no
